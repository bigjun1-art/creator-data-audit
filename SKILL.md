---
name: creator-data-audit
description: Batch-check creator or influencer data from authenticated marketing platforms and produce a verified evaluation table with evidence. Use for 巨量星图、抖音达人、达人价值核查、Chrome 登录态参数化后台批量抓取、达人名单补数、播放量日期截图、飞书达人评估表，以及后续小红书蒲公英达人查询、跨平台达人对比和批量核验任务。
---

# 达人数据核查

以最少页面操作完成可复核的达人数据表。两条及以上记录必须调用本 Skill 的可执行脚本，禁止重新手写抓取流程或逐个打开主页取数。

## 可执行入口（强制）

本 Skill 已内置可执行流水线：

- `scripts/run_creator_audit.py`：统一入口；`prepare` 生成固定 Chrome 作业，`finish` 完成校验、出图和可选飞书发布，`status` 读取断点状态。
- `scripts/discover_latest_lark_template.py`：只读核验飞书身份，自动发现最新已完成达人核查模板并输出真实列。
- `scripts/prepare_run.py`：解析 XLSX/CSV，生成运行配置、标准记录、检查点路径和一次性星图页面批量执行器。
- `scripts/xingtu_page_runner.template.js`：前两次成功任务验证过的同源后台路线；批量调用搜索、基础资料、最新作品三个接口，严格双重匹配并把结构化结果返回桥接器。
- `scripts/xingtu_chrome_bridge.mjs`：固定 Chrome 桥接器；认领已登录星图标签、执行后台批量代码、只续跑失败项、直接落盘结果、批量能力截图并释放标签。
- `scripts/run_xingtu_background.sh`：当前 Chrome 只读执行层的固定后台入口；锁定唯一已登录星图达人页，把 `prepare` 生成的执行器一次注入页面后台，等待结构化结果落盘，不占用鼠标或逐人开页。
- `scripts/validate_capture.py`：校验记录数守恒、ID 唯一、状态与列映射，生成最新版飞书数据。
- `scripts/make_evidence_charts.py`：按最近15条作品的真实播放量和发布日期生成证据图。
- `scripts/xingtu_ability_capture.mjs`：复用已登录 Chrome，按已核验星图ID批量判断真实顶部标签、提取地区、展开完整内容区并输出能力截图与断点清单。
- `scripts/write_lark_sheet.py`：串行创建飞书表、写值、上传图片、设置样式并回读验收。
- `scripts/upload_lark_images.py`：向已存在的飞书表串行补传能力截图，支持断点续传并按列回读图片对象数量。
- `scripts/self_test.py`：离线验证准备、执行器语法、计数守恒和日期证据图。

开始真实任务时只走统一入口和生成的 Chrome 作业，不重新研究接口或重写临时代码：

```bash
python3 scripts/run_creator_audit.py prepare --source INPUT.xlsx --out RUN --columns-json RUN/latest-template.json
# 当前 Chrome 页面执行层若限制长调用，执行 scripts/run_xingtu_background.sh --run-dir RUN；否则导入 RUN/chrome-job.mjs 并调用 default(chrome)。禁止临时写循环。
python3 scripts/run_creator_audit.py finish --out RUN --folder-token FOLDER --title TITLE
python3 scripts/run_creator_audit.py status --out RUN
```

`chrome-job.mjs` 是 `prepare` 自动生成的本次作业。Agent 在持久 JavaScript 会话中只执行固定调用：`job = await import("file:///绝对路径/chrome-job.mjs"); nodeRepl.write(await job.default(chrome));`。桥接器会直接写 `xingtu-capture.json`、`browser-result.json`、能力截图检查点和图片清单，不依赖浏览器下载弹窗。失败后再次调用同一作业，只补 `request_failed` 和未完成截图。

所有 `RUN`、`INPUT`、文件夹 token、标题和来源表都必须来自当前任务参数，不使用旧任务硬编码。

## 开跑提示

每次开始真实达人数据核查时，顺带提醒用户：`上次你说可能还要增加几个飞书表格列，这次需要补吗？` 这是非阻塞提醒；用户没有新增要求或暂未回复时，沿用现有列继续执行，不暂停任务。

## 平台路由

- 巨量星图：先读 [references/xingtu.md](references/xingtu.md)。浏览器阶段必须优先使用会话中可用的 `chrome:control-chrome` Skill，并先完整读取其指令。
- 小红书蒲公英：先读 [references/pugongying.md](references/pugongying.md)。在真实样本完成前只执行适配验证，不声称字段或接口已打通。
- 其他平台：按“平台接入”流程验证，新增独立 reference，不把某个平台的字段和接口直接套用。

## 参数化批量优先级

处理两条及以上记录时，先运行 `discover_latest_lark_template.py` 和 `run_creator_audit.py prepare`，再接触平台页面。未生成 `run-config.json`、`source-records.json`、`xingtu-page-runner.js` 和 `chrome-job.mjs` 前不得操作达人页面。

### 最新版列的确定规则

- 用户说“用最新版的列”“按之前最新表头”且未点名模板时，不得默认使用 `7.20达人价值核查` 或任何写死的日期子表。
- 先在用户指定的目标目录（默认“中转站”）搜索达人核查表，按最近编辑时间筛选，再读取候选工作簿的工作表名称、真实表头和已完成数据行；只把“列完整、已有成功核查数据、不是测试/草稿”的最近版本视为最新模板。
- 若多个候选表编辑时间接近，以列集合更完整、成功数据行更多者优先；用户明确指定的模板始终覆盖自动判断。
- 写入前必须回读并保存最新版模板的完整表头，建立“字段名 → 目标列”映射；新表必须复制整套最新版列及顺序，不凭记忆删列、加列或沿用旧列。
- `地区` 是固定第一列。即使最新版模板暂时没有，也要在运行配置中前置加入；平台主页显示地区就填写，未显示则留空，不按活动城市或用户所在地猜填。
- 向既有飞书表补第一列时，先回读并保存原 A 列。当前 `lark-cli sheets +dim-insert --position A` 的实际语义是在 A 后插入空白 B，不是“在 A 前插入”；正确做法是插入后把地区写入 A，并把保存的达人名称完整恢复到 B，再回读 `A1=地区`、`B1=达人名称` 和全部数据行的名称计数。禁止在未保存原 A 列时直接覆盖。
- “最新版”指最新完成且可复核的达人核查模板，不是目录中最近创建的任意电子表格，也不是空白测试表。

- 用户给出的表格、子表、列名和字段直接写入配置；未给出的值从源表及参考子表读取，不把旧任务的 URL、sheet ID、列号、达人名单或输出目录当默认值。
- 用当前登录态验证生成的执行器能返回一个真实样本；接口路径、请求体、无损64位ID解析、双重匹配、批次函数和检查点已经固化，不重新探索或重写。
- 同一批只导入并调用一次生成的 `chrome-job.mjs`，复用一个 Chrome 绑定和一个同源后台通道；固定桥接器执行 `xingtu-page-runner.js` 并输出统一记录，不为后台取数逐人开标签页。
- 页面内 `fetch` 或结构化读取受限时，允许在同一登录态中使用一个受控的同源 API 标签页，参数化替换请求中的达人ID或查询键并解析 JSON；不得导出 Cookie、鉴权头或改用独立浏览器。
- 批次完成后立即落盘成功、歧义和失败 ID；后续只运行未完成或失败 ID。上下文中断、标签页失效或接口变化时，从检查点续跑，不重抓已完成记录。
- 平台读取、身份核验、统计制图、网页截图和飞书写入分阶段执行。批量后台读取成功后，不得退回逐达人鼠标取数；只有平台真实截图字段使用脚本化页面导航。
- 写入前做全量断言：源记录数守恒、ID唯一、列映射完整、图片路径存在、普通缺失值与截图空白规则正确。飞书值批量写入，图片串行上传，随后按异常行加首尾样本回读。新建表由 `write_lark_sheet.py` 完成；既有表补图由 `upload_lark_images.py` 完成，禁止因补图重复建表。

## 标准流程

1. 读取用户原表，保留所有原字段，形成去重后的任务清单。
2. 用 `lark-cli sheets` 读取目标表和参考子表，按真实表头建立列映射；不要凭记忆新增、改名或错位覆盖列。
3. 在可写用户凭据的环境中串行检查 `lark-cli` 身份和令牌状态；只有身份正确且 `tokenStatus=valid` 才继续。飞书链接不在浏览器打开。
4. 使用 `chrome:control-chrome` 接入用户已登录的 Chrome，只导入并调用 `RUN/chrome-job.mjs`。桥接器自动认领星图标签、运行后台执行器、保存结果、批量截图并释放标签；不得在任务中重新编写浏览器循环。
5. 每条记录保存三种状态之一：`已核验`、`未唯一匹配`、`请求失败待重试`。只重试失败项。
6. `chrome-job.mjs` 完成后运行 `run_creator_audit.py finish`，一次性校验、统计、生成带日期的播放量证据图并可选发布飞书。能力截图由桥接器调用 `xingtu_ability_capture.mjs`，禁止逐个手工点击或重新手写循环。
7. 用户要求先看样例时，只完成指定达人并回读整行，等用户确认后再批量；不得提前处理后续达人。
8. 先批量写值，再串行上传图片；`lark-cli` 的授权检查、写表和图片上传禁止并发。
9. 写入后核对总数、成功数、歧义数、失败数、图片数及原字段保留情况，并回读首行、末行和异常行。只有名称、ID、数值、图片、空白列和计数一致才报告完成。

## 匹配规则

- 有平台账号ID：名称与账号ID或主页唯一ID双重核验。
- 无平台账号ID：用名称检索，结合原表粉丝量等已知信息筛选，再从后台回填ID。
- 严格区分原表“达人ID/抖音号”和“星图ID”；星图ID是平台内部标识，不能拿来冒充达人ID完成双重核验。
- 忽略不影响身份的表情和首尾空格，但保留原昵称与平台昵称。
- 同名候选无法唯一确定时标记 `未唯一匹配`，列出候选或缺失证据，不猜填。
- 不用相似账号补齐完成率。

## 证据和字段

- 第一列固定为地区；保留原表的达人名称、粉丝、费用、活动和来源等字段。
- 平台数据至少注明抓取时间；作品数据保留发布日期。
- 播放量图显示每条作品日期和统计覆盖范围，不能只显示序号。
- 后台接口生成的图标注为“基于平台后台作品数据生成”，不得冒充网页原始截图。
- 能力截图先用顶部标签判断类目是否真实存在，再按用户示例裁到该类目的完整数据内容区；成图不包含网站顶栏或“达人概览/商业能力/创作能力/履约能力/连接用户”等顶部标签栏。
- 能力截图复用黄金参数：宽度 1920、基础高度 2200、按 `.tabs-content` 实际高度动态扩到最多 6000、每位达人写 checkpoint 和 image manifest；不得再次临时研究裁切方法。
- 截图以目标内容元素为边界，不以当前可见窗口为边界。等待数据卡片和图表加载完成，处理滚动和懒加载后再截，禁止半屏、加载中、错标签或只有空白下半部的图片。
- 每个能力列只填平台实际显示的类目；没有独立类目或入口时留空，不用侧栏、概览卡片或其他截图代替。
- `创作能力-播放量（截图）` 是带日期的最近作品播放量证据；`创作能力` 是平台创作能力页的数据内容截图。两列不得互相替代。
- 表格昵称写平台主页完整名称，保留表情；写完必须回读姓名单元格，防止源表行错位。
- 平台未提供的普通文本字段写 `平台未提供`；能力截图类目未显示时直接留空；尚未核实的字段写 `待确认`。

## 最近15条播放量

1. 取创作能力页同一批最新15条作品，逐条保存播放量与发布时间。
2. 计算最低值、最高值和算术平均值；平均值按用户表格口径取整数。`w` 等展示值先转换为整数再统计。
3. 网页有“播放量”柱状图时截图数据内容区；网页新版只有点赞量/评论量时，不把点赞量图冒充播放量图。
4. 后台仍返回作品明细时，用15条播放量和对应日期生成核查图，图上明确写“数据来源：巨量星图创作能力页最近15条视频列表”，并在同一行写入最低、最高、平均播放量。
5. 日期必须与这15条作品一一对应。无法取得日期时不要生成带猜测日期的图，标记 `待确认`。

## Token 与操作预算

- 接口探路预算：默认 0 次重新探索。直接运行统一入口和生成的 Chrome 作业；只有固定桥接器明确返回接口结构变化时，才允许 1 个样本、1 次请求捕获、1 次响应验证并更新模板。
- 不逐个打开达人主页，不反复输出完整 accessibility tree、DOM、HAR 或 DevTools 文档。
- 页面状态只读取与当前元素有关的最小片段；结构变化或定位失败时才读取一次完整状态。
- 同一种工具参数错误最多纠正一次；先查看当前 Skill 或工具签名再继续。
- 保存已完成ID和批次状态；上下文压缩或中断后从状态继续，不重新探路。
- 浏览器只承担登录态、单样本请求验证和必须的页面截图；批量搜索、取数、统计、写表和上传都走代码，不用多个浏览器标签逐人操作。
- 不把 Computer Use、Playwright 点击或逐页导航当作批量抓取方案，也不启动独立 Chromium。后台同源执行通道不可用时立即熔断；只允许能力截图队列使用页面导航，绝不把第三次失败时的 UI 点击路线作为替代方案。
- 优先完成标志和增量结果，避免固定长等待与整批重跑。
- 进度只报告计数和阻塞点；最终只报告完成数、歧义数、失败数、表格链接、证据口径和待复核名单。

## 风控和失败处理

- 验证码、登录确认或平台安全校验出现时，暂停并请用户亲自完成；不要反复识别或点击。
- 接口结构变化时，只对一个样本重新捕获并更新适配规则。
- 批次失败时保留成功结果，只重试失败ID。
- `lark-cli` 返回 `20073` 或 `20064` 时立即停止写入，先检查用户凭据目录是否可写；不要连续重试或再次要求用户授权。授权成功后核对用户名、`open_id`、`tokenStatus` 和 `refreshExpiresAt`。
- 不读取、输出、保存或传递 Cookie、Token、密码和请求鉴权头。
- 不绕过平台权限、验证码、访问控制或频率限制。

## 平台接入

接入新平台时先完成一个真实样本：

1. 列出平台可见字段与用户需要字段的映射。
2. 确认账号唯一标识、名称歧义处理和作品时间口径。
3. 验证最小请求路径和证据图生成方式。
4. 记录平台限制、风控表现和安全等待间隔。
5. 用两条记录验证写表及回读后，再扩大批次。
6. 将已验证事实写入该平台 reference；未验证内容保留为待验证项。
